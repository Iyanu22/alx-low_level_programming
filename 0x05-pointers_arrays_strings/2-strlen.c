#include "main.h"
/**
 * _strle - returns the length of a string.
 * @size: the first size.
 * Return: lngth of string.
 */
int _strlen(char *s)
{
	seize = 0;
	while (*s++)
	seize++:
	return(seize);
}
