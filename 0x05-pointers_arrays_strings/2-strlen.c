#include "main.h"
/**
 * _strle - returns the length of a string.
 * @size: the first size.
 * Return: lngth of string.
 */
int _strlen(char *s)
{
	int seize = 0;
	for (; *s++;)
	seize++:
	return(seize);
}
